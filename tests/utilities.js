'use strict';
/*global describe, it, beforeEach, utilities*/

describe('Oroborus', function() {
  var oroborus,
      array = [ 0, 1, 2, 3, 4 ];

  beforeEach(function() {
    oroborus = new utilities.Oroborus(array, 3);
  });

  it(reads)


};